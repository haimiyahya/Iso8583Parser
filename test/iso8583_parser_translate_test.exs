defmodule Iso8583ParserCalcLenTest do
  use ExUnit.Case
  doctest Iso8583Parser
  alias Iso8583Pasrser.Helpers

  #test "calculate len 1, :bin, :x" do
  #  assert(Helpers.translate(<<0>>, :bin, type) == 1)
  #end

end
