defmodule Iso8583ParserCalcLenBinAplhaNumericTest do
  use ExUnit.Case, async: true
  doctest Iso8583Parser
  alias Iso8583Pasrser.Helpers

  test "calculate len 1, :bin, :a" do
    assert(Helpers.calc_len(1, :bin, :a) == 1)
  end

  test "calculate len 2, :bin, :a" do
    assert(Helpers.calc_len(2, :bin, :a) == 2)
  end

  test "calculate len 3, :bin, :a" do
    assert(Helpers.calc_len(3, :bin, :a) == 3)
  end

  test "calculate len 4, :bin, :a" do
    assert(Helpers.calc_len(4, :bin, :a) == 4)
  end

  test "calculate len 5, :bin, :a" do
    assert(Helpers.calc_len(5, :bin, :a) == 5)
  end

  test "calculate len 6, :bin, :a" do
    assert(Helpers.calc_len(6, :bin, :a) == 6)
  end

  test "calculate len 7, :bin, :a" do
    assert(Helpers.calc_len(7, :bin, :a) == 7)
  end

  test "calculate len 8, :bin, :a" do
    assert(Helpers.calc_len(8, :bin, :a) == 8)
  end

  test "calculate len 9, :bin, :a" do
    assert(Helpers.calc_len(9, :bin, :a) == 9)
  end

  test "calculate len 10, :bin, :a" do
    assert(Helpers.calc_len(10, :bin, :a) == 10)
  end

  test "calculate len 11, :bin, :a" do
    assert(Helpers.calc_len(11, :bin, :a) == 11)
  end

  test "calculate len 12, :bin, :a" do
    assert(Helpers.calc_len(12, :bin, :a) == 12)
  end

  test "calculate len 13, :bin, :a" do
    assert(Helpers.calc_len(13, :bin, :a) == 13)
  end

  test "calculate len 14, :bin, :a" do
    assert(Helpers.calc_len(14, :bin, :a) == 14)
  end

  test "calculate len 15, :bin, :a" do
    assert(Helpers.calc_len(15, :bin, :a) == 15)
  end

  test "calculate len 16, :bin, :a" do
    assert(Helpers.calc_len(16, :bin, :a) == 16)
  end

  test "calculate len 17, :bin, :a" do
    assert(Helpers.calc_len(17, :bin, :a) == 17)
  end

  test "calculate len 18, :bin, :a" do
    assert(Helpers.calc_len(18, :bin, :a) == 18)
  end

  test "calculate len 19, :bin, :a" do
    assert(Helpers.calc_len(19, :bin, :a) == 19)
  end

  test "calculate len 20, :bin, :a" do
    assert(Helpers.calc_len(20, :bin, :a) == 20)
  end

  test "calculate len 21, :bin, :a" do
    assert(Helpers.calc_len(21, :bin, :a) == 21)
  end

  test "calculate len 22, :bin, :a" do
    assert(Helpers.calc_len(22, :bin, :a) == 22)
  end

  test "calculate len 23, :bin, :a" do
    assert(Helpers.calc_len(23, :bin, :a) == 23)
  end

  test "calculate len 24, :bin, :a" do
    assert(Helpers.calc_len(24, :bin, :a) == 24)
  end

  test "calculate len 25, :bin, :a" do
    assert(Helpers.calc_len(25, :bin, :a) == 25)
  end

  test "calculate len 26, :bin, :a" do
    assert(Helpers.calc_len(26, :bin, :a) == 26)
  end

  test "calculate len 27, :bin, :a" do
    assert(Helpers.calc_len(27, :bin, :a) == 27)
  end

  test "calculate len 28, :bin, :a" do
    assert(Helpers.calc_len(28, :bin, :a) == 28)
  end

  test "calculate len 29, :bin, :a" do
    assert(Helpers.calc_len(29, :bin, :a) == 29)
  end

  test "calculate len 30, :bin, :a" do
    assert(Helpers.calc_len(30, :bin, :a) == 30)
  end

  test "calculate len 31, :bin, :a" do
    assert(Helpers.calc_len(31, :bin, :a) == 31)
  end

  test "calculate len 32, :bin, :a" do
    assert(Helpers.calc_len(32, :bin, :a) == 32)
  end

  test "calculate len 33, :bin, :a" do
    assert(Helpers.calc_len(33, :bin, :a) == 33)
  end


end
