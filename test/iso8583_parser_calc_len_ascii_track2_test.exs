defmodule Iso8583ParserCalcLenAsciiTrack2Test do
  use ExUnit.Case
  doctest Iso8583Parser
  alias Iso8583Pasrser.Helpers

  test "calculate len 1, :ascii, :z" do
    assert(Helpers.calc_len(1, :ascii, :z) == 1)
  end

  test "calculate len 2, :ascii, :z" do
    assert(Helpers.calc_len(2, :ascii, :z) == 2)
  end

  test "calculate len 3, :ascii, :z" do
    assert(Helpers.calc_len(3, :ascii, :z) == 3)
  end

  test "calculate len 4, :ascii, :z" do
    assert(Helpers.calc_len(4, :ascii, :z) == 4)
  end

  test "calculate len 5, :ascii, :z" do
    assert(Helpers.calc_len(5, :ascii, :z) == 5)
  end

  test "calculate len 6, :ascii, :z" do
    assert(Helpers.calc_len(6, :ascii, :z) == 6)
  end

  test "calculate len 7, :ascii, :z" do
    assert(Helpers.calc_len(7, :ascii, :z) == 7)
  end

  test "calculate len 8, :ascii, :z" do
    assert(Helpers.calc_len(8, :ascii, :z) == 8)
  end

  test "calculate len 9, :ascii, :z" do
    assert(Helpers.calc_len(9, :ascii, :z) == 9)
  end

  test "calculate len 10, :ascii, :z" do
    assert(Helpers.calc_len(10, :ascii, :z) == 10)
  end

  test "calculate len 11, :ascii, :z" do
    assert(Helpers.calc_len(11, :ascii, :z) == 11)
  end

  test "calculate len 12, :ascii, :z" do
    assert(Helpers.calc_len(12, :ascii, :z) == 12)
  end

  test "calculate len 13, :ascii, :z" do
    assert(Helpers.calc_len(13, :ascii, :z) == 13)
  end

  test "calculate len 14, :ascii, :z" do
    assert(Helpers.calc_len(14, :ascii, :z) == 14)
  end

  test "calculate len 15, :ascii, :z" do
    assert(Helpers.calc_len(15, :ascii, :z) == 15)
  end

  test "calculate len 16, :ascii, :z" do
    assert(Helpers.calc_len(16, :ascii, :z) == 16)
  end

  test "calculate len 17, :ascii, :z" do
    assert(Helpers.calc_len(17, :ascii, :z) == 17)
  end

  test "calculate len 18, :ascii, :z" do
    assert(Helpers.calc_len(18, :ascii, :z) == 18)
  end

  test "calculate len 19, :ascii, :z" do
    assert(Helpers.calc_len(19, :ascii, :z) == 19)
  end

  test "calculate len 20, :ascii, :z" do
    assert(Helpers.calc_len(20, :ascii, :z) == 20)
  end

  test "calculate len 21, :ascii, :z" do
    assert(Helpers.calc_len(21, :ascii, :z) == 21)
  end

  test "calculate len 22, :ascii, :z" do
    assert(Helpers.calc_len(22, :ascii, :z) == 22)
  end

  test "calculate len 23, :ascii, :z" do
    assert(Helpers.calc_len(23, :ascii, :z) == 23)
  end

  test "calculate len 24, :ascii, :z" do
    assert(Helpers.calc_len(24, :ascii, :z) == 24)
  end

  test "calculate len 25, :ascii, :z" do
    assert(Helpers.calc_len(25, :ascii, :z) == 25)
  end

  test "calculate len 26, :ascii, :z" do
    assert(Helpers.calc_len(26, :ascii, :z) == 26)
  end

  test "calculate len 27, :ascii, :z" do
    assert(Helpers.calc_len(27, :ascii, :z) == 27)
  end

  test "calculate len 28, :ascii, :z" do
    assert(Helpers.calc_len(28, :ascii, :z) == 28)
  end

  test "calculate len 29, :ascii, :z" do
    assert(Helpers.calc_len(29, :ascii, :z) == 29)
  end

  test "calculate len 30, :ascii, :z" do
    assert(Helpers.calc_len(30, :ascii, :z) == 30)
  end

  test "calculate len 31, :ascii, :z" do
    assert(Helpers.calc_len(31, :ascii, :z) == 31)
  end

  test "calculate len 32, :ascii, :z" do
    assert(Helpers.calc_len(32, :ascii, :z) == 32)
  end

  test "calculate len 33, :ascii, :z" do
    assert(Helpers.calc_len(33, :ascii, :z) == 33)
  end

  test "calculate len 34, :ascii, :z" do
    assert(Helpers.calc_len(34, :ascii, :z) == 34)
  end

  test "calculate len 35, :ascii, :z" do
    assert(Helpers.calc_len(35, :ascii, :z) == 35)
  end


end
